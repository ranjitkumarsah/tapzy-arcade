// Display-only mirror of the server perk catalog (server/products.js is the
// authoritative source of cost). Used to render prices; the server re-checks.
export const PERKS = {
  continue: { cost: 25, label: 'Continue' },
}
